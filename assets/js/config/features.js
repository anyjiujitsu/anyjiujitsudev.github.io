// assets/js/config/features.js
// purpose: single-source feature flags for optional UI/modules

export const FEATURES = {
  // Floating + button (FAB) that opens the "Add Entry" modal shell
  FAB_ENTRY: true,
};
